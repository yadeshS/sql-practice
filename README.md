# sql-practice

# SQL Practice 📊

This repository contains my **daily SQL practice**, where I consistently work on SQL fundamentals, queries, and real-world use cases.

The goal of this repository is to build a **strong foundation in SQL** for data analysis, backend development, and cloud-related roles.

---

## 📌 Topics Covered

- SQL Basics (SELECT, INSERT, UPDATE, DELETE)
- Filtering Data (WHERE, AND, OR, LIKE)
- Sorting & Limiting Results (ORDER BY, LIMIT)
- Aggregations (COUNT, SUM, AVG, MIN, MAX)
- Grouping Data (GROUP BY, HAVING)
- Joins (INNER JOIN, LEFT JOIN, RIGHT JOIN)
- Constraints (PRIMARY KEY, FOREIGN KEY, UNIQUE)
- SQL Functions (String, Date, Numeric)
- Subqueries & Nested Queries

---

## 📂 Folder Structure

```text
sql-practice/
│
├── basics/           # Basic SQL commands
├── queries/          # Filtering, sorting, grouping
├── joins/            # Join operations
├── constraints/      # Keys and constraints
├── functions/        # Built-in SQL functions
├── notes/            # Concept explanations
└── README.md
